node_modules
.env
.env.*
!.env.example
.DS_Store
dist
build
.next
.vercel
.turbo
coverage

*.tsbuildinfo

# Local API runtime data
# Keep the demo seed file committed for now; revisit once DB lands.

data/smoke-test-requests.json
